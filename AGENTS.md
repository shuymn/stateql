# Agent Policy

## sqldef Reference Boundary

- `reference/sqldef/**` is reference-only and must not be edited.
- New implementation code must be added under `crates/**`.
- If upstream files are copied into this repository, keep license/attribution files alongside the copied content.
